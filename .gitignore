# Sensitive files and credentials
.env
.env.local
.env.*.local
*.key
*.pem
*.crt
credentials.json
secrets.json
.credentials
config.local.*
.config.local
oauth_tokens.json

# IDE files
.vscode/
.idea/
*.swp
*~
.DS_Store

# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.pytest_cache/
.coverage
htmlcov/

# Runtime output written by running the test suite (StatGuardian/quality
# contract validation logs), not source
terrain_validations/

# Compiled PyO3 extension module (built locally by `maturin develop`;
# published builds go through `maturin build` -> dist/, also ignored above)
*.so
*.pyd
*.dylib
python/pyterrain_map/*.abi3.so

# Virtual environments
venv/
env/
.venv

# Rust
target/
Cargo.lock

# Temp files
*.tmp
*.log
temp/
tmp/
